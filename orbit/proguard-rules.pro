# EasyTier Android ProGuard Rules
# Currently minification is disabled (isMinifyEnabled = false).
# These rules are reserved for future minification; keeping the file
# present ensures build.gradle.kts references don't break when
# minification is enabled.
